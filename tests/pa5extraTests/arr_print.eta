use io
use conv

main(args: int[][]) {
	arr: int[] = {0,1,2,3,4,5}
	println(unparseInt(length(arr)))
	printArr(arr)
}

printArr(arr:int[]){
	i : int = 0
	while(i<length(arr)){
		print(unparseInt(arr[i])+" ")
		i = i + 1
	}
}