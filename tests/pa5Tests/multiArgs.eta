use io
use conv
main(args:int[][]){
    a:int,b:int,c:int = square(10);
    println(unparseInt(a));
    println(unparseInt(b));
    println(unparseInt(c));
    //println(unparseInt(d));
}

square(n1:int):int,int,int{
    return n1,n1,n1;
}
