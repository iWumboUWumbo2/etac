use io
use conv
main(args:int[][]) {
    println(unparseInt(fib(10)))
}

fib(n:int) : int {
    if (n == 0) {return 1}
    if (n == 1) {return 1}
    println(unparseInt(n))
    return fib(n-1) + fib(n-2)
}
