use io
use conv

a1 : int[]
a2 : int[][]

main(args: int[][]) {
	a1  = {1,2,3}
	a2  = {{4}, {5, 6, 7}}
	a3 : int[123]
	a4 : int[12][4]

	prInt(length(a1)) // 3

	prInt(length(a2)) // 2
	prInt(length(a2[1])) // 3

	prInt(length(a3)) // 123

	prInt(length(a4)) // 12
	prInt(length(a4[0])) // 4
}

prInt(x:int){
	println(unparseInt(x))
}