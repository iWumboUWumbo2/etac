use io
use conv
g : int = 0;
b1 : bool = true;

main (a : int[][]) {
	g = -12;
	println(unparseInt(g))
	printBool(b1)
}

printBool(b:bool){
  if(b){
    println("true")
  }
  else{
    println("false")
  }
}