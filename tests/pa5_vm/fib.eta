use io
use conv
main(args:int[][]) {
    println(unparseInt(fib(1)))
    println(unparseInt(fib(2)))
    println(unparseInt(fib(10)))
    println(unparseInt(fib(25)))
}

fib(n:int) : int {
    if (n == 0) {return 1}
    if (n == 1) {return 1}
    return fib(n-1) + fib(n-2)
}
