use io
use conv

main(args: int[][]) {
	arr: int[] = {0,1,2,3,4,5}
	i : int = 0
	println(unparseInt(length(arr)))
	while(i<length(arr)){
		//print(unparseInt(arr[nextOf(i-1)])+" ")
		print(unparseInt(arr[i])+" ")
		i = i + 1
	}
}

nextOf(a:int):int{
	return a+1
}