use io
use conv
main(args: int[][]) {
	a : int[] = {inc(1),inc(2)}	
	printArr(a)
}

inc(n:int):int{
	println("hoho "+unparseInt(n))
	return n + 1
}

printArr(arr:int[]){
	i : int = 0
	while(i<length(arr)){
		print(unparseInt(arr[i])+" ")
		i = i + 1
	}
}