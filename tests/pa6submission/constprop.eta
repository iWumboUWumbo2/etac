use io
use conv

main(args:int[][]) {
    a:int = 10;
    b:int = 5;
    i:int = 0;
    j:int = 0;
    c:int = 0;

    while (i < a) {
        j = 0;
        while (j < b) {
            c = c + i * j;
            j = j + 1;
        }
        i = i + 1;
    }

    println(unparseInt(c))
}