
use io
use conv;

main(args:int[][]){
    a:int = 5;
    c:int = 1;
    b:int[20];
    while (c < length(b)){
        x:int = a;
        y:int = x;
        g:int = x + y;
        c = c + c + g;
    }
    a = c - a;
    c = 0;
    println(unparseInt(a));
}