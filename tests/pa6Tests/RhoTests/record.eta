record Point {
    x,y,z: int
}

record OrderMatters{
    x: int
    y: bool
}

foo() {
    y = Point(1, 2, 3);
    z : int = y.x;
    z = Point(1,2,3).x;
    a : OrderMatter = OrderMatters(true, 3) // this should fail typecheck
}