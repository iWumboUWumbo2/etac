use io
use conv

record Point {
    x : int
    y : int
}

record PointArr {
    x : Point[];
}

foo(a : int, b:int) : int[] {
    c : int[] = {a,b}
    return c;
}

main(args : int[][]) {
    a : Point = Point(2,3);
    b : Point = Point(4,5)
    c : PointArr = PointArr({a,b})

    //foo(3,4)[0] = 5;
    c.x[0].x = 2;
    //PointArr({a,b}).x[0].x = 2;
    // no type decl (PointArr, {a,b})
    // arraccesdecl (x, {0})
    // no type decl (x)
}