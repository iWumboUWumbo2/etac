use io
use conv

record Point {
    x : int
    y : int
}

record PointArr {
    x : Point[];
}

main(args : int[][]) {
    a : Point = Point(2,3);
    b : Point = Point(4,5)
    c : PointArr = PointArr({a,b})

    //c.x[2].x = 2;
    PointArr({a,b}).x[0].x = 2;
    // no type decl (PointArr, {a,b})
    // arraccesdecl (x, {0})
    // no type decl (x)
}