record Point {
    x : int
    y : int
}

main(args : int[][]) {
    a : Point = Point(2,3);
    b : Point = Point(4,5);
    c : Point = Point(6,7);

    e : int[a.y][a.x];
    e[0][0] = b.x;

    f : int[] = {b.y, c.x};

    g : Point[] = {a, Point(111111,111111111111), c}

    h : Point[a.y];
    h[0] = c;

    i : int[] = {g[0].x, g[1].y, g[2].x, g[2].y};

}