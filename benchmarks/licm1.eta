
use io
use conv;
g':int = 5;
main(args:int[][]){
    a:int = 5;
    c:int = 1;
    b:int[20];
    while (c < length(b)){
        b = b + {1}
        c = c + 2
    }
    a = c - a;
    c = 0;
    println(unparseInt(a));

    i:int = 0;
    res:int = 0;
    while (i < 200102930){
        x:int = 123123123/a;
        y:int = 192837192837/length(b)
        res = res + x + y;
        i = i + 1;
    }
    println(unparseInt(res));
        println(unparseInt(g'));
}