use io
use conv
g':int = 5;

main(args:int[][]) {
    x : int = 2
    y : int = 3
    z : int = 4;
    dud:int = 5;
    i:int = 0;
    while (i < 152387108){
        y = z / 7;
        y = z / 5;
        i = i + 1;
    }
    x = 5;
    println(unparseInt(x));println(unparseInt(g'));
}