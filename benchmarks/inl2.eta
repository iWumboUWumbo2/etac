use io;use conv;g':int = 5;
main(args: int[][]) {
  i:int  = 0;
  res:int = 0;
  while (i < 130000000){
      i = i +1;
      res = res + foo(i);
      res = res - foo(i-1);
  }
  println(unparseInt(res));
    println(unparseInt(g'));

}
foo(n:int):int{
    return inc(n);
}
inc(n:int):int{
    return n + 1;
}